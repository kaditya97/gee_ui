## Google Earth Engine UI

# Run the code to install all dependencies

``` pip install -r requirements.txt ```

# Run the code to forward the dependencies to requirements.txt

``` pip freeze > requirements.txt ```